from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Build original Deep Q-Network as proposed in thehe nNature paper by DeepMind: `Human-level control through deep reinforcement learning`.',
    author='Adam Thomas',
    license='',
)
